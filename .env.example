# ── Obligatoires ──────────────────────────────────────────────
OPENAI_API_KEY=sk-...
SECRET_KEY=change-me-in-production
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Stripe (optionnel tant que billing non activé) ─────────────
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PRICE_ID=price_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Email via Brevo (optionnel — fallback : lien en JSON) ──────
BREVO_API_KEY=xkeysib-...
SMTP_FROM=ton@email.com
APP_URL=https://ton-app.onrender.com

# ── Modèle / comportement (optionnel) ─────────────────────────
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=
# BOT_TEMPERATURE=0.4
# BOT_MAX_TOKENS=300
# CHAT_RATE_LIMIT=100

# ── Chemins (optionnel) ────────────────────────────────────────
# PORT=8000
# FRONTEND_DIR=../frontend
